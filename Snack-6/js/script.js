// Stampa il cubo dei primi N numeri,
// dove N è un numero indicato dall’utente attraverso un prompt.

var numero = parseInt(prompt('inserisci numero'));

for(n = 1; n <= numero; n++){
}   document.getElementById('cubo').innerHTML = Math.pow(numero, 3);
